# Auto detect text files and perform LF normalization
* text=auto
  1. Поменять местами содержимое переменных A и B и вывести новые
значения A и B.
#include <math.h>
int main() {
// int A = 5;
// int B = 2;
// int C = A;
// A = B;
// B = C;
// std::cout << "A = " << A << "\n";
// std::cout << "B = " << B << "\n";
}
2. Даны переменные A, B, C. Изменить их значения, переместив содержимое
A в B, B — в C, C — в A, и вывести новые значения переменных A, B, C.
#include <iostream>
int main2(){
int A = 6;
int B = 4;
int C = 8;
int temp1 = B;
B = A;
int temp2 = C;
C = temp1;
A = temp2;
 std::cout << "A = " << A << "\n";
 std::cout << "B = " << B << "\n";
 std::cout << "C = " << C << "\n";
}
3. Даны переменные A, B, C. Изменить их значения, переместив содержимое
A в C, C — в B, B — в A, и вывести новые значения переменных A, B, C.
#include <iostream>
int main3(){
int A = 6;
int B = 4;
int C = 8;
int temp1 = B;
B = A;
int temp2 = C;
C = temp1;
A = temp2;
 std::cout << "A = " << A << "\n";
 std::cout << "B = " << B << "\n";
 std::cout << "C = " << C << "\n";
}
4. Найти значение функции y = 3x6
 − 6x2
 − 7 при данном значении x
#include <iostream>
#include <math.h>
int main4(){
int X = 3;
int y = 3 * pow(X,6) - 6 * pow(X,2) - 7;
 std::cout << y;
}
5. Найти значение функции y = 4(x−3)6
 − 7(x−3)3
 + 2 при данном значении x
#include <iostream>
#include <math.h>
int main5(){
int X = 5;
int y = 4 * pow(X-3,6) - 7 * pow (X-3,3) + 2;
 std::cout << y;
}
6. Дано число A. Вычислить A8
 , используя вспомогательную переменную и
три операции умножения.
#include <iostream>
#include <math.h>
int main6()
{
int A = 8;
int B = A * A;
 B = B * B;
 B = B * B;
 std::cout << B << "\n";
 B = A * A;
 std::cout << B << "\n";
 B = B * B;
 std::cout << B << "\n";
 B = B * B;
}
7. Дано число A. Вычислить A15
, используя две вспомогательные переменные
и пять операций умножения.
#include <iostream>
#include <math.h>
int main7()
{
int A = 15;
int B = A * A;
 B = B * B;
 B = B * B;
int C = B * B;
 C = B * C;
 std::cout << B << "\n";
 B = A * A;
 std::cout << B << "\n";
 B = B * B;
 std::cout << B << "\n";
 B = B * B;
 std::cout << B << "\n";
 C = B * B;
 std::cout << B << "\n";
}

